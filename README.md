# googlesheet-to-json

Get your googlesheet api credentials, add the api user to googlesheet, input your googlesheet api credentials in the code, give a file name and vola!! you can get the data in json using this code.

